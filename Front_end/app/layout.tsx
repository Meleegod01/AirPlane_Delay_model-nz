import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Flight Delay Predictor - Advanced Aviation Analytics",
  description:
    "AI-powered flight delay prediction system with real-time analytics and insights for aviation operations.",
  keywords: "flight delays, aviation, machine learning, predictions, airlines, airports",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="dark">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
